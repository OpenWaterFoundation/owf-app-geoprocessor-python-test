StartLog(LogFile="results/test-SetProperty.gp.log")
# Simple test to make sure command runs
# - need to enhance to set different properties and write to a file
# Uncomment the following line to regenerate expected results
SetProperty(PropertyName="TestProperty",PropertyType="str",PropertyValue="Test string value1")
